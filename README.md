# SPENGA × Apex MD — spenga.apexmd.com

Static partner microsite. Edit the `*.html` files directly; there is no build step.

- **Deploy:** Vercel. Repo root is the site root — leave Vercel's Root Directory blank.
- **Brand tokens:** `spenga-theme.css` (`:root`). Green is `#6FA02F` deep /
  `#96CA4F` bright-on-dark, per `SPENGA-Package/README.md`.
- **Intake:** `https://formspenga.apexmd.com/?categoryId=<cat>` —
  `weight-loss | microdosing | trt | hrt | longevity | bloodwork`.
- **Affiliate:** iDevAffiliate id **172** (`api/track.js`), fired by `affiliate.js`.
- **Copy rule:** Apex MD is NOT bundled into SPENGA membership. Use
  "SPENGA has partnered with…" / "exclusive member access".
  Never "included with your membership".
